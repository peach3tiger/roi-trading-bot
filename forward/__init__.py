"""forward — forward test: ghi log tín hiệu/allocation mỗi bar, KHÔNG đặt lệnh.

Xem forward/README.md và forward/logger.py cho chi tiết. Package rỗng có
chủ đích — mọi logic nằm trong logger.py để `tests/test_forward_logger.py`
import trực tiếp (`from forward.logger import ...`).
"""

from __future__ import annotations
