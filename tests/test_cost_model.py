"""tests.test_cost_model — phí tính đúng. Implement ở Phase 4.

Sau khi implement, file này KHÔNG được skip, không được xfail, không
được comment out (CLAUDE.md bất biến #15).
"""

import pytest


def test_rebalance_cost_includes_fee_and_slippage() -> None:
    pytest.skip("TODO: Phase 4 — backtest/cost_model.py")


def test_total_cost_report_pct_of_gross_profit() -> None:
    pytest.skip("TODO: Phase 4 — báo cáo phí theo USDT và % lợi nhuận gộp")


def test_assume_taker_fee_when_configured() -> None:
    pytest.skip("TODO: Phase 4 — assume_taker: true dùng taker_fee_pct luôn")
